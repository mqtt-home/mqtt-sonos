project_name: mqtt-sonos
version: 2

before:
  hooks:
    - go mod tidy
    - sh -c "cd web && npm install -g pnpm@11.5.1 && pnpm install --frozen-lockfile && pnpm run build"

builds:
  - id: mqtt-sonos
    main: ./main.go
    binary: mqtt-sonos
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mqtt-home/mqtt-sonos/version.Version={{.Version}} -X github.com/mqtt-home/mqtt-sonos/version.GitCommit={{.Commit}} -X github.com/mqtt-home/mqtt-sonos/version.BuildTime={{.Date}}

dockers:
  - image_templates:
      - "pharndt/mqtt-sonos:{{ .Tag }}-amd64"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    goos: linux
    goarch: amd64
    build_flag_templates:
      - "--platform=linux/amd64"
    extra_files:
      - web/dist/
    skip_push: false

  - image_templates:
      - "pharndt/mqtt-sonos:{{ .Tag }}-arm64"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    goos: linux
    goarch: arm64
    build_flag_templates:
      - "--platform=linux/arm64"
    extra_files:
      - web/dist/
    skip_push: false

docker_manifests:
  - id: tag
    name_template: "pharndt/mqtt-sonos:{{ .Tag }}"
    image_templates:
      - pharndt/mqtt-sonos:{{ .Tag }}-amd64
      - pharndt/mqtt-sonos:{{ .Tag }}-arm64

  - id: latest
    name_template: "pharndt/mqtt-sonos:latest"
    image_templates:
      - pharndt/mqtt-sonos:{{ .Tag }}-amd64
      - pharndt/mqtt-sonos:{{ .Tag }}-arm64

release:
  disable: false
  mode: replace
  draft: false
  prerelease: auto
  make_latest: true
  skip_upload: true
